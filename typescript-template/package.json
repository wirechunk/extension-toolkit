{
  "name": "<name>",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --experimental-transform-types --env-file .env --env-file .env.local --watch ./src/main.ts",
    "db:init": "node --experimental-transform-types --env-file .env --env-file .env.local ./scripts/init-db.ts",
    "db:migrate": "npm run db:init && prisma migrate dev",
    "typecheck": "tsc",
    "typecheck-src": "tsc --skipLibCheck",
    "format": "prettier --write .",
    "test": "vitest run",
    "postinstall": "touch .env.local"
  },
  "dependencies": {
    "@fastify/formbody": "^7.4.0",
    "@prisma/client": "^5.17.0",
    "@wirechunk/extension-toolkit": "0.0.6"
  },
  "devDependencies": {
    "@types/node": "^22.7.7",
    "@types/pg": "^8.11.10",
    "pg": "^8.12.0",
    "prettier": "^3.3.3",
    "prisma": "^5.17.0",
    "typescript": "^5.6.3",
    "vitest": "^2.0.5"
  }
}
